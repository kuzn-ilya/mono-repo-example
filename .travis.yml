language: node_js

node_js:
  - '8'

install:
  - npm install

script:
  - npm run build

after_success:
  - cat ./coverage/lcov.info | ./node_modules/codecov/bin/codecov

deploy:
  provider: script
  skip_cleanup: true
  script:
    - npx travis-deploy-once "npx semantic-release"
